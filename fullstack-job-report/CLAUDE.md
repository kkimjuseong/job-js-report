# 풀스택 개발자 채용 공고 데일리 브리핑

## 목적
매일 오전 7시(KST), **2~3년차 풀스택(백엔드·프론트엔드·모바일) 개발자가 지원할 만한 한국 채용 공고**를 모아
한국어로 정리한 뒤 사용자의 카카오톡(나에게 보내기)으로 전송한다.

> 이 파일은 Cloud Routine이 매번 읽는 작업 지침서다.
> 비밀값(`KAKAO_REST_KEY`, `KAKAO_CLIENT_SECRET`, `KAKAO_REFRESH_TOKEN`)은
> 클라우드 환경변수로 주입돼 있고 `send_kakao.py`가 자동으로 사용한다. (`.env` 아님)

## 대상 직무 (4개 분야, 분야별 고루 수집)
- **백엔드**: 백엔드 개발자, 서버 개발자, API 개발
  - 키워드: Java/Spring, Kotlin, Node.js/NestJS, Python/Django/FastAPI, Go, MySQL/PostgreSQL, Redis, MSA
- **프론트엔드**: 프론트엔드 개발자, 웹 개발자
  - 키워드: React, Next.js, TypeScript, Vue, 웹 퍼블리싱 제외
- **모바일**: iOS 개발자, Android 개발자, 앱 개발자 (네이티브 + 크로스플랫폼 모두 포함)
  - 키워드: Swift/SwiftUI, Kotlin/Jetpack Compose, React Native, Flutter
- **풀스택**: 풀스택 개발자, 웹 서비스 개발자 (백+프론트 동시 요구)
- 분야별 최대 3개, 총 5~10개. 한 분야에 몰리지 않게 한다.

## 경력 기준 (중요)
2~3년차가 현실적으로 지원 가능한 공고만 고른다.
- **포함**: 경력 1~4년, "주니어~미들", "경력 3년 이상", "경력 무관"
- **제외**: 신입 전용·인턴, 시니어/리드/팀장/매니저급(대개 5년+ 요구), 헤드·CTO급
- 애매하면 포함하되 요구 경력을 함께 표기한다.

## 대상 사이트  ← 원하는 대로 이 목록만 수정하면 된다
- 원티드  https://www.wanted.co.kr
- 잡코리아 https://www.jobkorea.co.kr/
- 사람인 https://www.saramin.co.kr/
- 리멤버 https://career.rememberapp.co.kr/
<!-- 특정 회사 채용 페이지를 넣고 싶으면 여기에 URL을 추가한다. 예: 토스, 쿠팡, 당근 -->

## 수집 방식 (중요 — 스크린샷 금지, 데이터 추출)
공고를 모을 때 **스크린샷/이미지 캡처로 화면을 읽지 않는다.** 느리고 토큰을 많이 쓴다.
대신 아래 우선순위로 **구조화된 데이터를 직접 추출**한다.
1. **사이트 내부 JSON API 직접 호출** (1순위, 가장 빠름)
   - 검색·필터 결과는 결국 API가 내려준다. `requests`(클라우드) 또는 브라우저 `fetch()`로
     해당 API를 호출해 JSON으로 받는다. 지역·경력 필터는 API/URL 파라미터로 서버에서 먼저 좁힌다.
   - 예) 원티드: `https://www.wanted.co.kr/api/v4/jobs?query=백엔드&years=1&years=4&...`
2. **페이지 DOM에서 직접 추출** (2순위)
   - 브라우저로 열어야 하는 SPA는 `javascript_tool`로 DOM을 한 번에 긁어 제목/회사/경력/링크를 뽑는다.
   - 무한 스크롤은 스크린샷 없이 JS로 스크롤+추출한다.
3. 스크린샷은 **로그인/캡차/레이아웃 확인 등 꼭 필요할 때만** 최소로 쓴다.
- 사이트마다 로그인이 필요한지 먼저 확인한다(예: 링크드인은 세션 필요).
- 여러 동작은 한 번에 묶어 호출한다(round-trip 최소화).

## 실행 단계
1. `pip install -r requirements.txt`
2. 위 '대상 사이트'에서 '대상 직무' 4개 분야 공고를 '수집 방식'(데이터 추출)으로 검색한다.
3. '경력 기준'에 맞는 공고만 추린다. 분야별·사이트별로 고루, 최근 공고 위주로 총 5~10개.
4. 아래 '출력 형식'으로 한국어 다이제스트를 만든다.
5. `python send_kakao.py "<다이제스트 전문>"` 으로 전송한다. (인자=argv로 전달, 파이프 금지: 윈도우 파이프는 한글이 깨진다)

## 출력 형식
```
📅 {오늘 날짜} 풀스택 채용 공고 브리핑

[번호/전체개수] {분야}
[회사명] 직무명
  - 규모 : {중소, 중견, 스타트업, 대기업, 공기업 등 회사 규모}
  - 경력 : {요구 경력}
  - 핵심 : {주요 기술스택 / 자격 한 줄}
  - 모집 기간 : {시작일 ~ 종료일}
  - 링크 : {공고 URL}

...
```
- `{분야}`는 백엔드 / 프론트엔드 / 모바일 / 풀스택 중 하나.
- `모집 기간`이 공고에 없으면 "상시채용" 또는 "마감일 미정"으로 표기한다.

## 규칙
- 한국어로 작성한다.
- 광고/홍보/외주·프리랜서 모집 글, 퍼블리셔·QA·기획 등 개발 외 직무는 제외하고 정규직 개발 채용 위주로 고른다.
- 같은 공고가 여러 사이트에 있으면 한 번만 (중복 제거).
- 경력 기준에 안 맞는 공고는 넣지 않는다.
- 공고를 충분히 못 찾으면 개수를 줄여도 되지만, **빈 브리핑은 보내지 않는다.**
- 각 공고에는 반드시 지원 가능한 링크를 포함한다.
- **메시지 길이 한도**: 카카오 텍스트 메시지 1건의 한도는 약 **1000자**다.
  다이제스트를 가능한 한 **하나의 메시지로** 보내되, 1000자를 넘으면 **공고 단위로 묶어**
  다음 메시지로 넘긴다(공고가 중간에 잘리지 않게). `send_kakao.py`가 자동으로 분할한다(`TEXT_LIMIT`).
- `send_kakao.py` 실행이 실패하면 에러 메시지를 로그에 남기고 종료한다.
